import { ScrollSpyItemDirective } from './scroll-spy-item.directive';

describe('ScrollSpyItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSpyItemDirective();
    expect(directive).toBeTruthy();
  });
});
