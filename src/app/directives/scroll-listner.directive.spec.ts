import { ScrollListnerDirective } from './scroll-listner.directive';

describe('ScrollListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollListnerDirective();
    expect(directive).toBeTruthy();
  });
});
